# Status

The recurrent-maintenance extension is implemented on this branch and is ready for pull-request CI. The next acceptance gate is a green `lake build`; no claim should be promoted to `main` before that check passes.
