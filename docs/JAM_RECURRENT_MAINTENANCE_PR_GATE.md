# PR acceptance gate

Before merge:

- Lean target compiles in GitHub Actions;
- existing Python tests remain green;
- paper compiles if/when a paper-build check is run;
- claims remain scoped as model extensions;
- no production-JAM phase assignment is made without grounding data.
