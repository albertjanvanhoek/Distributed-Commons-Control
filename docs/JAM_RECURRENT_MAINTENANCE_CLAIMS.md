# Recurrent-maintenance claim boundary

This note is intentionally redundant with the paper ledger so the new layer is easy to audit.

## Machine-checked within the declared model

- canonical replacement iff the three slow-loop couplings cover the product of maintenance deficits;
- forward invariance above the canonical witness for nonnegative coefficients;
- all-time positivity of the canonical trajectory under the strict loop condition and positive deficits/couplings;
- event-level correction does not imply recurrent maintenance;
- recurrent maintenance does not imply event-level correction;
- satisfying current correction plus the local compute-selection boundary does not imply slow maintenance;
- all four fast/slow phase cells have explicit witnesses.

## Not machine-checked or not claimed

- that the current JAM staking/operator/client ecosystem has the proposed three-variable linear dynamics;
- numerical values of the slow-loop couplings;
- that every relevant maintenance route passes through all three proposed capacities;
- that recurrent maintenance implies truth, security against every fault, or protocol optimality;
- novelty of the underlying product/spectral threshold mathematics.

## Scientific claim

The proposed JAM-facing contribution is the decomposition itself:

```text
current fault-specific corrective reproduction
    !=
reproduction of the capacity to remain corrective across future audits.
```

That decomposition becomes testable once the delegated higher-layer mappings are specified or measured.
